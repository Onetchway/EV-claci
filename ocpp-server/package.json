{
  "name": "livanto-ocpp-server",
  "version": "0.1.0",
  "private": true,
  "description": "OCPP 2.0.1 Central System (CSMS) for Livanto Green — charge point connection lifecycle, live station status, session/transaction logging. Phase 1: no remote commands yet.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.4.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
