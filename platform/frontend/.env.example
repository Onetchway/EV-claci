NEXT_PUBLIC_API_URL=http://localhost:5100/api

# Display only — shown as the suffix when previewing a tenant's subdomain
# URL. Should match platform/backend's PLATFORM_BASE_DOMAIN.
NEXT_PUBLIC_BASE_DOMAIN=alpha.app
